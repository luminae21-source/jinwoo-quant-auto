# 진우퀀트 — 6월말 첫 측정 실행 체크리스트 (정확판, 2026-06-13)

> 트리거: 진우 "6월말 작업을 정확하게, 준비 안 된 건 마저". 병목 = **운영단계 첫 데이터**.
> 이번 세션에 **준비물 신규 제작 완료**: `trackw_compute.py`(반사실 자동계산)·`build_data_json.py`(통합 어댑터, 실데이터·KOSPI+KOSDAQ). 둘 다 self-test 통과.
> 원칙: production 무수정. backtest≠forward. 매수·W실현수익 = 진우, 계산·기록 = 도구/Claude.

---

## 0. 한 줄
6월말에 **① T1 집행(매도+매수) → ② 월간가격 갱신 → ③ 3개 측정파일 기입(2개는 도구가 자동) → ④ data.json 생성**. 이 네 단계가 운영단계의 첫 데이터를 만들고 통합-0 트리거의 절반을 채운다.

---

## 1. 준비물 상태 (이번 세션 점검·제작)

| 도구 | 역할 | 상태 |
|---|---|---|
| `live_tracker.py --add` | 포트 전체 vs KOSPI alpha | ✅ 기존(수동: 포트%·KOSPI%) |
| **`trackw_compute.py`** | **sys_ret·themeEW_ret 실데이터 자동계산** | ✅ **신규 제작**(self-test OK) — 손계산 병목 해소 |
| `trackw_score.py` | Track W 재량기여 판정(6개월~) | ✅ 기존(ledger 읽기) |
| **`build_data_json.py`** | 통합 허브용 data.json(양 시장) | ✅ **신규 제작**(self-test OK, 실데이터 검증) |

### ⚠️ 정리 필요 (준비 안 됐던 것 2건)
1. **Track W ledger 중복** — `trackw_ledger.csv`(소수단위, trackw_score용)와 `track_w_record.csv`(%단위, track_w_v1용)가 **같은 걸 두 번** 기록. → **`trackw_ledger.csv`를 정본**으로(06-13 핸드오프·신규 도구가 이걸 씀), `track_w_record.csv`+`track_w_v1.py`는 구버전 은퇴(삭제 말고 방치 무해). **진우 확인 후 단일화.**
2. **6월 월간가격 = stub** — `kospi_monthly_prices.csv`의 2026-06 값이 진행중 stub(6/12 기준 등). **6월말 마지막 거래일 종가로 갱신해야 반사실 확정**(아래 2단계).

---

## 2. 6월말 실행 절차 (순서대로 — 명령 수준)

### ① T1 집행 (진우 직접, 장중)
- dashboard(또는 로컬 `python score_v37_2.py`)로 당일 S+/S/A 13종 확인.
- 에코프로머티 1/3 매도 + 적립 50만 → B전환계획 §4 빌드룰로 7종 매수(NH·두산·아모레·KT&G·ISC·한미·NAVER).
- ⚠️ 한미반도체는 1Q 어닝쇼크(영업익 −88%, HBM4 지연) 감안 — 뺄지 진우 판단(8/17 2Q 리마인더 대기).
- 매수/매도가·수량 기록.

### ② 데이터 갱신 (월말 마지막 거래일 후, PC)
```
python score_v37_2.py            # 6월말 등급·월간가격 확정 (월간패널 6/30 반영)
python score_v39_pead.py         # v3.9 병행관찰 1줄
```
(GitHub Actions가 매일 돌므로 보통 자동 — 월말 종가 반영분 확인만)

### ③ 측정 3종 기입
```
# (a) 포트 전체 vs KOSPI — 진우가 실제 포트 6월수익%·KOSPI 6월수익% 입력
python live_tracker.py --add 2026-06 <포트수익%> <KOSPI수익%>

# (b) Track W 반사실 — sys/themeEW는 자동, W는 진우 실현수익
python trackw_compute.py --month 2026-06 --write
#   → trackw_ledger.csv에 sys_ret·themeEW_ret 자동기입(실데이터). W_ret=대덕 default.
#   → 진우: 실제 재량(대덕+진입테마) 실현수익으로 W_ret 칸 확인·수정.

# (c) 판정 리포트(아직 1개월이라 "표본 부족" 정상)
python trackw_score.py
```
- 실전기록(`진우퀀트_v37_2_실전기록.md`) §3-1 트랙분리 3줄(S/W/H)도 같이 기입.

### ④ 통합 어댑터 생성 (통합-0 첫 산출)
```
python build_data_json.py        # → data.json (시장 양지수 MA200·시스템13·테마게이트·TrackW)
```
- 이게 통합-0의 데이터 절반. Track W 1~2개월 더 쌓이면 허브 HTML이 이 data.json만 읽어 그림(블루프린트 §5).

---

## 3. 이번 측정이 동시에 여는 것 (왜 병목인가)
- (a) **운영단계 첫 데이터** — '만든 것'을 '돌려 재기' 시작.
- (b) **통합-0 트리거 절반** — Track W ≥1개월 충족 시작(나머지=시장스냅샷, ④가 채움).
- (c) **재량 vs 시스템 측정 시작** — W−sys = 재량(대덕·테마)이 밥값 하나, 6~12개월 누적 판정.

## 4. KOSPI + KOSDAQ 양 시장 — 맞음 (확인)
- **시스템 트랙**: KOSPI 11 + KOSDAQ 2(알테오젠·ISC) = production 13픽 그대로. (KOSPI-only 정책은 *체계적 팩터 배분*에 한함 — KOSDAQ 룰선정만 0.)
- **테마 lane**: KOSDAQ 전용(forward 재량).
- **시장 조망**: data.json이 **KS11·KQ11 둘 다** MA200·MTD 산출(이번 실측: 코스피 MA200 위·MTD −4.2% / 코스닥 MA200 위·MTD −4.3%).
- → 통합 허브는 **양 시장 모두** 다룬다. 단 "체계적 알파 배분"은 KOSPI, "테마 재량"은 KOSDAQ로 *역할 분리*.

## 5. 정직 가드
- 6월 수치는 월말 확정 전까진 stub(진행중). 첫 기입은 월말 종가 후.
- sys/themeEW는 실데이터 확정이나 **W_ret는 진우 실현수익이 정답**(default 대덕은 출발점). 사후 합리화 금지(보유기간 변형 사전등록 그대로).
- 1개월 표본은 판정 불가 — 6~12개월 누적이 결정(느낌 아님).
- 연구단계는 "완료"가 아니라 **계속**: 미해결(격월 영역3 재판정·Block Bootstrap·로봇 펀더 심화·눌림 재게이트)은 진행.

---
작성: 2026-06-13. 도구 신규 2종 self-test 통과·실데이터 검증. 첫 실행 = 6월말.
