# 진우퀀트 강화 키트 — 수익엔진·집행설계 (2026-07-23)

"검증된 수익엔진(C)·리스크 집행 설계(B)가 약하다"에 대한 답. **새 연구·새 데이터 없이 진우님 자료만으로** 귀속·통합·강제. 전부 self-test 통과. ⚠️ 투자자문 아님·책임 본인.

## 설치
`강화키트/` 폴더를 진우퀀트 루트 안에 두세요(상위에 backtest JSON·월봉캐시·RiskGuard 등이 있어야 자동 인식). `pip install pandas numpy`.

## 파일

### A. 수익 엔진 귀속 — `알파_3분해.py`  (self-test 3/3)
v3.7.2 초과수익을 유니버스/선정·틸트로 분해. 백테 JSON의 실현 월수익 사용(재실행 불필요).
```
py 강화키트\알파_3분해.py
```
결과(진우 데이터): 총 +43.1%p vs KOSPI = 유니버스 +18.8%p(44%) + 선정·틸트 +24.3%p(56%, IR vs EW +0.40). Mom +6.08·BAB −1.87(진우님 수치와 일치). → **본체 v3.7.2 = 검증된 수익 엔진.**

### B. 매도규칙 v3 통일 — `매도규칙_라우터.py` (11/11) + `매도규칙서_v3_통합정본.md`
진입 유형별 청산: core=MA200+등급 · momentum=넓은트레일(3.5ATR/−25%, 2.5ATR 금지) · deepvalue=타이트無·재난−40%·가치익절(이격≥1.5/+100%/PBR≥1.0).
```
py 강화키트\매도규칙_라우터.py --self-test
from 매도규칙_라우터 import check_exit
```
⚠️ 지난 `매도규칙_v2.py`(균일 2.5ATR)는 §9 검증서 휩쏘 판명 → 폐기·대체.

### C. 한도 통합 — `진우_통합한도.json` + `진우_한도로더.py` (7/7) + `집행_런북.md`
모든 리스크 파라미터를 단일 JSON으로. per-trade risk 1%(최신 정본) 채택.
```
py 강화키트\진우_한도로더.py --show
py 강화키트\진우_한도로더.py --self-test
```

### D. 계절틸트 종결 — `계절틸트_재검정_2026-07-23.md`
지수 검정(채택후보) vs 30년 패널(기각) 충돌 → **기각 유지** 확정. 통합전략 v1만 stale(정정 필요).

## 진우님이 할 것 (아침, 5~15분)
1. `my_holdings.csv`에 진입일 채우기 → Track W 잠금 해제
2. `진우퀀트_통합매매전략_v1_2026-07-12.md`에서 계절틸트 "채택"→"기각·무코어(안 D)" 정정
3. `py 강화키트\진우_한도로더.py --show`로 통합 한도 확인
4. (선택) `progress`: `execute.py --broker kis --paper`로 페이퍼 집행 착수

## 두 방어선 (혼동 금지)
- **사이징_강제.py**(실행키트) = 전략 규모(얼마 살까) · **RiskGuard**(진우_리스크차단기) = 집행 직전 방어. 순서: 사이징→주문생성→RiskGuard→승인→집행.
