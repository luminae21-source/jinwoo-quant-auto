# 진우퀀트 — 수익 레버 2개: grade-cut(검증완료) + v3.9 EarnMom(검증 필요)

> 2026-06-03 · 수익 추구 철학 기반. "검증 안 된 건 검증하고 진행" 방침.

---

## A. grade cut S+/S — ✅ 이미 검증됨 (진우 결정만 남음)

진우 님 자신의 `validate_robustness`(2026-05-29) 결과:

| 등급 컷 | 연환산 | Sharpe | MDD | 4년 누적 |
|---|---|---|---|---|
| **S+/S/A (현 production)** | 71.54% | **2.78** | **−12.21%** | 805.7% |
| **S+/S (공격형)** | **81.25%** | 2.57 | −14.33% | 1034.1% |
| S+ only | 79.52% | 2.03 | −18.68% | 990.4% |

→ **S+/S로 좁히면 4년 CAGR +9.7%p.** 대가: MDD −12.2%→−14.3%, Sharpe 2.78→2.57, **종목 수 감소로 반도체 집중 ↑**. 수익 추구 철학엔 부합.

**적용 방법** (live 변경이라 진우 결정):
```python
# score_v37_2.py  compute_scores() 안
target_grades = {'S+', 'S', 'A'}   # 현재
target_grades = {'S+', 'S'}        # 공격형으로 변경
```
권장 절차: ① 먼저 `python score_v37_2.py`로 S+/S 픽 목록·섹터 집중 확인 → ② 괜찮으면 GitHub Actions 반영. (되돌리기 = 한 줄 원복) **원하시면 제가 토글 플래그로 안전하게 코드 넣어드립니다.**

---

## B. v3.9 EarnMom (실적 모멘텀 / PEAD) — ⚠️ 검증 필요 → 하네스 작성 완료

**신호**: YoY ΔROE (이번 분기 ROE − 전년 동분기 ROE) → 횡단면 3분위 +1/0/−1 (Echo와 동일 방식, 가중 1.0).
**근거**: Bernard-Thomas PEAD + Kim-Lee-Min 2019(한국). 좋아지는 실적이 주가에 천천히 반영된다는 것.
**데이터**: `quality_timeseries_summary.csv`(이미 보유, 분기 ROE) + FDR 가격. **추가 수집 불필요.**
**PIT 차단**: 각 분기는 공시월 이후만 사용(Q1 5/15·Q2 8/14·Q3 11/14·연간 익년 3/31). v3.8.2 AG가 lookahead로 실패한 교훈 반영.
- EPS Revision은 애널 컨센서스 데이터가 없어 **제외**(실적 모멘텀으로 대체).

**실행** (진우 환경):
```powershell
cd "C:\Users\긍정적인_삶의자세\Desktop\진우퀀트"
python backtest_v39_pit.py
```
출력: `base(v3.7.2)` vs `+EarnMom×1.0` vs `+EarnMom×0.5` 의 연환산·Sharpe·MDD·IR.

**합격선** (v3.8과 동일 기준): `+EarnMom 연환산 ≥ base AND MDD 악화 없음` → **통과 시 v3.9 채택**, 아니면 **기각**(GP/AG처럼). self-test 5개 통과(synthetic).

**정직한 prior**: 한국 18종목 대형주는 GP·AG를 거부했습니다. EarnMom도 같은 운명일 수 있어요. 그래서 *기대*가 아니라 *검증*으로 판단합니다. 결과 표 주시면 채택/기각 판정해 드립니다.

---

## C. 권장 순서

1. **B 먼저 실행** (`python backtest_v39_pit.py`) → 표 공유 → 채택/기각 판정. (공짜 검증, 리스크 0)
2. **A는 병행 가능** — 공격형 원하시면 S+/S 픽 먼저 확인 후 결정.
3. 둘 다 끝나면 영역 3(반도체 집중)은 "리스크가 더 걱정될 때" 카드로 보류.

> 메모: grade-cut은 *수익을 더 짜내는* 레버(검증됨), v3.9는 *새 알파를 더하는* 레버(검증 중). 둘은 독립이라 같이 적용해도 됩니다 — 단 둘 다 반도체 집중·MDD를 키우는 방향이라 그 점만 인지.
