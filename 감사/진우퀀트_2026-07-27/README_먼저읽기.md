# 📦 진우퀀트 2026-07-27 산출물 — Cowork 인계 패키지

> 2026-07-27 Claude 채팅 세션의 전 산출물. **Cowork에서 이어가기 위한 패키지.**
> ⚠️ 정보·검증용 · 투자자문 아님

---

## ⚡ 3분 안에 시작하기

### 1단계 — 압축 풀기
`Desktop\진우퀀트\감사\` 아래에 풀면 된다:
```
Desktop\진우퀀트\감사\진우퀀트_2026-07-27\
```

### 2단계 — Cowork 열기
Claude Desktop 앱 → **Cowork** 탭 → 작업 폴더를
`C:\Users\<사용자>\Desktop\진우퀀트` 로 지정.

### 3단계 — 아래를 그대로 붙여넣기

```
진우퀀트 프로젝트야. 작업 폴더는 Desktop\진우퀀트.

먼저 이것부터 읽어줘:
  감사\진우퀀트_2026-07-27\01_인계\진우퀀트_세션정리_2026-07-27_최종판.md

요약하면 — 2026-07-27에 백테 엔진에서 룩어헤드 편향을 발견했다.
시총 파일의 date가 월말 스냅샷인데 백테가 그걸로 같은 달 수익률을 계산해서,
사후에 승자를 자동 선택하고 있었어. 교정하니 30년 성과가 20.9% → 4.9%로 떨어졌고
백서 3장·9장이 무효화됐다. 팩터 계열 6개 스크립트는 감사 통과(무죄).

그리고 forward 트랙을 PC 자립으로 바꿨다. 종전엔 신호 생성이 샌드박스 경로에
묶여 있어서 PC에서 실행이 안 됐는데, 경로 자동탐색 + 패널 생성기 신규 작성으로 해결했어.

다음 작업은 세션정리 §8 로드맵이고, 8/1엔 §7 실행 순서를 따르면 돼.
파일들 확인하고 지금 상태 정리해줘.
```

**파일을 첨부할 필요 없다.** Cowork는 폴더를 직접 읽는다.

---

## 📁 폴더 구성

| 폴더 | 내용 | 배치 위치 |
|---|---|---|
| `01_인계` | **세션정리 최종판** — 여기부터 읽기 | 그대로 |
| `02_백서` | 백서 소스 + 차트 생성기 | → `백서_빌더\` |
| `03_forward` | forward 4종 (신호·벤치·패널·사양서) | → 루트 / `실전준비\` |
| `04_감사도구` | 룩어헤드·문서·데이터 감사기 9종 | → 루트 |
| `05_보고서` | 발견 경위·판정 근거 6편 | 그대로 (참조용) |
| `06_재량트랙_복리` | 재량 트랙 사양·복리 시뮬 | → `실전준비\재량트랙\` |

### 이미 PC에 배치된 것 (중복 주의)
어제 이미 옮긴 파일들이다. 덮어쓰기 전 날짜 확인:
```
lookahead_shift_test.py · audit_whitepaper.py · 클레임_레지스트리.csv
build_style_panel.py · forward_benchmark.py
백서_빌더\make_charts_v2.py · 백서_빌더\진우퀀트_백서.md
실전준비\forward_signal.py · 실전준비\forward_동결사양서_v1.1.md
```

---

## 🎯 8/1 실행 순서 (가장 중요)

```powershell
cd "$env:USERPROFILE\Desktop\진우퀀트"

py fetch_fundamental_panel.py --start 2026    # ① 재무 (KRX 로그인 승인)
# ② 가격 — 데이터수리\ 폴더 수집기
py build_style_panel.py --compare             # ③ 패널 → 2026-07
py 실전준비\forward_signal.py                  # ④ 신호 → 원장 2개월차
py forward_benchmark.py                       # ⑤ 벤치마크 B1·B2
```

**확인:** ③ `기간 ~ 2026-07` · ④ `as-of 2026-07` · ⑤ 2개월 표

> ⚠️ **`진우퀀트_월간갱신.bat`은 이 흐름이 아니다.**
> 그건 워치리스트·attribution·투자보드 계열이며 `종목재무_KRX_*.csv`를 갱신하지 않는다.
> forward 재무 수집기는 **`fetch_fundamental_panel.py`**.

---

## 🔑 핵심 결론 5줄

1. **30년 백테 무효** — 시총 룩어헤드. 20.9%→4.9%. 결함은 `유니버스_규칙화_검정.py`·
   `유니버스_리더십로테이션.py` 2개 파일에 국한.
2. **팩터는 생존** — `factor_efficacy.py` 등 6종 감사 통과(`fwd=RET.shift(-1)`).
   배당 +3.4% · B/P +6.6% · IC HAC t 4.10/3.34/2.36 유효.
3. **데이터 인프라 결백** — 시총 PIT 정상 · 상폐 45% 포함 · 재무 PIT 시차 정상.
4. **검증한 모델 ≠ 운용하는 모델** — 롱숏 5분위를 검증하고 롱온리를 운용한다.
   전략 총수익을 뒷받침하는 검증이 현재 없다.
5. **범용 탐지법** — 선택 변수에 `shift(1)`을 넣어 성과가 3%p 이상 급락하면 미래 정보.
   `lookahead_shift_test.py`. **정적 분석으로는 못 잡는다.**

---

## 📌 다음 작업 (세션정리 §8)

| # | 작업 | 소요 |
|---|---|---|
| 1 | 롱온리 백테 재산출 (조정본·정상시차·EW 대조군) | 반나절 |
| 2 | 백테를 15종·실제 사이징으로 재현 | 반나절 |
| 3 | 비용 모델 통일 (0.003 / 0.001 / 0.48% 병존) | 반나절 |
| 4 | 3·9장 재산출 후 무효 표기 해제 | 반나절 |
| 5 | 빌드 게이트 상설화 | 30분 |
| 6 | **forward 6~12개월 실측** | 달력 |

**6번만이 진짜 검증이다.**

---

## 💡 Cowork로 옮기는 이유

| | Claude 채팅 | Cowork |
|---|---|---|
| 실행 환경 | 격리 컨테이너 | **PC에서 직접** |
| 파일 접근 | 업로드한 것만 | 폴더 직접 읽기·쓰기 |
| 전달 | 카드 → 다운로드 → 수동 이동 | 그 자리에서 저장 |

어제 세션의 상당 시간이 파일 업로드·다운로드 왕복에 쓰였다.
진우퀀트처럼 **여러 파일을 읽고 고치고 검증하는 반복 작업**은 Cowork가 맞다.
