# 진우퀀트 영역 3 확장 모듈 — 결정메모 (Stage 0)

> 작성: 2026-06-05 (영역 3 모듈 세션) / 상태: **✅ Stage 0 승인 완료 (2026-06-05 진우) — §2~§8 변경 금지 발효**
> 모듈 D(v4.0 regime, 2026-06-05 기각·종결) §10의 승인된 후속: **② DART 전 종목 자동화 ③ 비용·합격선 재설계 + regime 재시험 1회**
> 원칙: production(score_v37_2)·C(v3.9 관찰)·D 산출물 무수정, 신규 파일만. 합격선은 이 메모 승인 후 변경 금지 (C·D 전례).

---

## 1. 범위·출발 상태 (2026-06-05 실측 확인)

- universe: `universe_rule30_latest.csv` (30종목, 2026-06-05 진우 승인 확정 — 현 스냅샷 전원 KOSPI, KOSDAQ 0)
- **데이터 레이어 완비** — 신규 DART 수집 없이 백테스트 착수 가능:
  - `fundamentals_pit.csv` 577종목 2019~2025 (universe 30/30 커버, null ≈ 0. HD현대마린솔루션만 2개년 — 2024 상장, 정상)
  - `fundamentals_kosdaq.csv` 297종목 / 가격 월간패널 584종목(KOSPI 355+KOSDAQ 229, 2019-12~2026-06)
  - regime 캐시 양쪽 보존: `regime_market_cache_v40.json`(KOSPI) + `regime_market_cache_v40_kosdaq.json`(KOSDAQ, 어긋남 29% 실측)
- 룰 풀은 pooled(KOSDAQ 포함) → **과거 PIT 멤버십에는 KOSDAQ 종목이 등장 가능** → 시장별 비용·detector 라우팅을 백테스트에 실장 (현 스냅샷이 KOSPI 100%라도 설계는 시장 라우팅).

## 2. 사전 등록 ① — 점수 정의 (2026-06-05 진우 채택: **PIT-proxy**)

`pit_universe_backtest.score_at`과 동일 정의, 공식 엔진에서는 월별 산출:

| 컴포넌트 | 정의 | 범위 |
|---|---|---|
| 체력 | F(정수 Piotroski 0~9, FY−1) × 12/9 | 0~12 |
| Sloan | accrual = (NI−CFO)/assets, 5분위 (낮을수록 +) | +2~−2 |
| NOA | noa_ratio 5분위 (낮을수록 +) | +2~−2 |
| Mom12 | production 동일 구간 매핑 (`_mom_score`) | +2~−2 |
| BAB | β60 production 동일 구간 매핑 (`_bab_score`) | +2~−2 |
| Echo | t−12~t−7 수익률 rank ≥0.8 → +1 / ≤0.2 → −1 | +1~−1 |

- 등급·선별 = production 컨벤션 그대로: `grade()` 절대 컷, picks = {S+, S, A}(점수 ≥9), EW + caps(종목 15%·섹터 35%), picks 0인 달은 현금(0%). **컷 튜닝 금지.**
- 채택 사유: F_korean 연속값(6.21 등)은 산출 스크립트가 저장소에 없는 Colab 수작업 유산 → 복제 시 정의 드리프트 위험. PIT-proxy는 universe 선정·검증과 동일 정의 + fundamentals_pit로 완전 자동화. ModF·FAR 생략(보조 항 전례, PIT검증 설계 §5).
- **명시: PIT-proxy ≠ production 점수.** univ30은 별도 트랙 — 18종 production 점수표와 직접 비교 금지.

## 3. 사전 등록 ② — 백테스트 구조 (공식 엔진)

- **2층 구조**: 멤버십 = 연 1회 5월 PIT 로테이션 (pooled 풀·월간패널, pooled top-30 룰 = universe확정 메모 §7 그대로) / 실행 = 월초 리밸 공식 엔진 (backtest_v39_pit_pead·v40 컨벤션 1:1 — MS→bfill, 마지막 부분구간 포함, 비용×턴오버, caps, metrics/IR = backtest_v37_2 import)
- 윈도: FDR 일별 4년 (fixed18 전례 동일), 벤치마크 KOSPI. PIT 보장: 리밸일 d0에서 ≤d0 데이터만, 재무는 FY−1 (5월 로테이션 = 3월 사업보고서 공시 후)
- **동시점 병행 6 arms** (한 실행에서 전부 산출): KOSPI / fixed18@0.235% (재현 게이트) / univ30 base / univ30 regime_w / univ30 regime_s / univ30@0.235% (참고)

## 4. 사전 등록 ③ — 비용 (시장별 라우팅)

| 시장 | 왕복 | 근거 |
|---|---|---|
| KOSPI | **0.35%** | 현행 0.235%의 보수화 — 30종은 시총 top-200 중형 포함 (18종 초대형과 다름) |
| KOSDAQ | **0.60%** | D §10 등록 prior 0.5~0.7%의 중간값 |
| 참고 병기 | 0.235% | fixed18 전례 비교용 — **판정에 미사용** |

적용: 종목별 시장 라벨로 턴오버에 차등. 판정은 0.35/0.60% 라인으로만.

## 5. 사전 등록 ④ — 합격선 (승인 후 변경 금지)

**A. universe 게이트** (2026-06-05 진우 선택: **시장 상대**) — 같은 실행의 KOSPI 라인 대비, 셋 다 충족 시 PASS:

1. CAGR ≥ KOSPI +3.0%p
2. IR ≥ 0.30
3. MDD ≤ KOSPI (시장보다 비악화)

FAIL → 모듈 종결. 룰 universe는 분기 재스크린 정보 트랙으로만 유지(운용 미편입), 재시험·재튜닝 금지.

**B. regime 게이트** (D §2 재사용, 진우 확정) — **univ30 base 대비**, 셋 다 충족 시 PASS:

1. MDD ≥ +2.0%p 개선
2. CAGR ≥ −1.0%p
3. Sharpe·IR ≥ base −0.01

변형 2개(regime_w/s)만, `REGIME_MULTS`·hysteresis·detector 임계값 무수정, **재시험 1회 한정**. 둘 다 FAIL → regime 종결(18종 기각과 합산 기록). regime 채택은 A PASS 전제 — A FAIL 시에도 산출은 기록(정보용).

**C. PASS 시**: production 즉시 교체 없음 — C 패턴 **병행 관찰 모드**부터.

## 6. 사전 등록 ⑤ — regime 시장 라우팅

- 종목 시장별 detector: KOSPI 종목 ← KOSPI 캐시 / KOSDAQ 종목 ← KOSDAQ 캐시 (어긋남 29% 실측이 라우팅 근거)
- 5요소 중 시장 3요소(trend·vol·flow)만 시장별 분리, 섹터집중·팩터 신호는 전략 레벨 공유 (D 백테스트 구조 동일 — 순환 참조 방지 포함)
- prev_state는 변형 × 시장별로 리밸 경로 따라 순차 유지 (hysteresis 경로의존)

## 7. 사전 등록 ⑥ — 재현 게이트 (D §8 교훈 반영)

- **절대값 참조 게이트 금지** (참조 CAGR은 기록 시점 시장에 종속 — 2026-06 급락 오작동 전례).
- (1) 구조 게이트: fixed18 라인의 컴포넌트 합 == 공식 `compute_scores_at` (매 리밸 assert) (2) 모든 비교는 같은 실행 내 delta로만 해석.
- 2026-06 미완성 stub: 전 arms 동일 적용 → 판정 유효 (universe확정 메모 §5-5 전례).

## 8. 사전 등록 ⑦ — 분기 재스크린 운용 룰

같은 파이프라인 재실행(fetch → score_inputs → 룰 스크린), 분기당 교체 ≤2 (universe_rules MAX_SWAP_Q 계승), 갱신 시 `universe_rule30_latest.csv` 교체 + 1줄 기록. 사람 검토 포인트(조선·중공업 그룹 집중 등)는 universe확정 메모 §7 유지 — 재량 제외는 기록 남기고 최소화.

## 9. 산출물 (신규 파일만, 전부 self-test 동반)

| Stage | 파일 | 역할 | 실행 |
|---|---|---|---|
| 1 | (신규 없음) `fetch_dart_fundamentals_pit.py` **무수정 재사용** | 확인 결과 `--codes-csv`(universe CSV)·`--market KOSPI,KOSDAQ`·`--out` 증분 옵션 기존 내장 → 범용 수집기 신규 작성 불필요 (단순함 우선). 분기 갱신: `--codes-csv universe_rule30_latest.csv --start-year <직전년> --out fundamentals_univ_increment.csv` | PC, 분기 1회 |
| 1 | `make_score_inputs.py` | fundamentals 3원천 병합(증분>pit>kosdaq) → 종목·연도별 F·Sloan·NOA + 시장·섹터 라벨 캐시 (`piotroski()` 재사용) | 샌드박스 가능 |
| 2 | `score_univ30.py` | 점수·등급·caps 정의 단일화 — 백테스트·live 분기 운용 공용 import (v40 패턴) | 양쪽 |
| 3 | `backtest_univ30.py` | 공식 엔진 6 arms + 게이트 자동 판정 (synthetic self-test) | **PC, 판정** |
| 4 | 이 메모 §11 판정 기입 + PASS 시 관찰 모드 가이드 | — | — |

## 10. 정직한 prior·한계 (사전 기록)

1. pooled_top30은 비용 무차감 PIT 엔진에서 IR 0.07 ≈ 0 — 본 시험의 실질 질문은 "**월별 멀티팩터 선별(grade cut)이 룰 universe에서 엣지를 추가하는가**". 기각 가능성 상당, 등록 기각 누적 7건의 연장선임을 인지하고 시작.
2. fixed18(73% CAGR)과의 직접 경쟁 아님 — 다른 트랙. 비교는 정보용.
3. regime: 18종 기각과 동일 메커니즘 — 회의적 prior 유지. 30종(중형 포함) + 시장 라우팅이 유일한 신규 정보.
4. survivorship: 시장라벨·풀 = 현재 스냅샷 (상장폐지·이전상장 미반영 — 기존 sweep·PIT 한계 동일).
5. 가드레일(시총·ADTV)은 운용 단계 전용 — 백테스트 멤버십은 점수+정적 라벨만 (look-ahead 방지, universe확정 메모 §5-1).
6. grade cut(≥9)은 production 절대 컷 차용 — PIT-proxy 분포에서 picks 수가 월별로 출렁일 수 있음. 컷 튜닝 금지, picks 0 달은 현금. 결과 해석 시 평균 picks 수 병기.

## 11. 판정 기록 (실행 후 기입)

- [x] Stage 0 진우 승인 ✅ 2026-06-05 — 점수 정의 PIT-proxy·합격선 시장 상대·비용 0.35/0.60%·2층 구조 포함 전체 승인. 선행 확인: 영역 1(추가 퀀트 기법)은 v3.7.2로 공식 완료 상태라 본 모듈의 선행 조건 아님 (의존 방향이 오히려 역 — 새 기법 재시험은 확장 universe가 전제)
- [ ] Stage 1~2 self-test 통과
- [ ] Stage 3 synthetic self-test 통과
- [ ] PC 실행 (`python backtest_univ30.py`) → 게이트 자동 판정
- [ ] 공식 판정 기입 → PASS: 관찰 모드 설계 / FAIL: 종결 (재튜닝 금지)

---
작성: 2026-06-05. §2~§8은 승인 후 변경 금지.
