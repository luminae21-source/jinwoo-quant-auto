# ⚠️ 이 파일은 버그의 잔재입니다 — 삭제하세요

이건 **결과가 아닙니다.** 경로 버그가 있던 옛 `할로윈_검증.py`가
데이터를 못 찾고 "판정불가(데이터부족)" 스텁을 **엉뚱한 폴더(`엔진\`)** 에 써놓은 것입니다.

- **진짜 결과 위치**: `가상매매\검증\할로윈_검증결과.md`
- 버그: `BASE = 엔진폴더` 로 잡혀 있어 진우퀀트 루트의 `kospi_index_daily.csv` 를 못 찾음
- 수정: 2026-07-13 — `jq_paths.py` 도입. 데이터는 ROOT(진우퀀트), 산출물은 `가상매매\검증\`

**이 파일은 지워도 됩니다.** 남겨두면 진짜 결과와 헷갈립니다.
